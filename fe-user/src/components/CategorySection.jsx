const CategorySection = ({ title }) => {
  return (
    <section style={{ marginBottom: "32px" }}>
      <h2>{title}</h2>
      <p>Danh sách sách sẽ hiển thị ở đây</p>
    </section>
  );
};

export default CategorySection;
